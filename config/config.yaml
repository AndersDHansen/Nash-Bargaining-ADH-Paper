defaults:
  - paths: default
  - scenario_gen: default
  - contract: baseload
  - opt_params: default
  - sensitivity: default
  - _self_

barter: true

# Boolean gate for the sensitivity pipeline.
# Named 'run_sensitivity' to avoid shadowing the 'sensitivity' config group above.
run_sensitivity: true
