defaults:
  - paths: default          # File system paths for raw data, processed outputs, results
  - scenario_gen: default   # Monte Carlo generation and reduction settings
  - experiment: default_baseload # Full run spec: contract + model params
  - sensitivity: bargaining_power    # Use default (none) for a single run, or name a sweep config
  - plotting: default       # Figure colours, geometry and fonts
  - _self_
